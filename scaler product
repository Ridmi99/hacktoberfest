#include<stdio.h>
int main()
{
    int a[i];b[i],c[i],x,y,sum1,sum2;
    printf("Enter the size of arrays: ");
    scanf("%d",&x);

    for(y=0;y<x;y++)
    {
        printf("Enter a value to the number 1 element %d: ",y+1);
        scanf("%d",&arr1[y]);
        printf("Enter a value to the number 2 element %d: ",y+1);
        scanf("%d",&arr2[y]);
        //scaler sum
        sum1=sum1+a[i];
        sum2=sum2+b[i];
        //vector sum
        c[i]=a[i]+b[i];
        //scaler product
        c[i]=a[i]*b[i];
        sp=sp+c[i];
        //vector product
        c[i]=a[i]*b[i];
    }
    printf("\n scaler product is %d \n vector product is %d ",)


    }
}
